name: nodejs6-thumbnail # 项目模板的名字 
displayName:  图像压缩 # 项目模板展示在控制台的名称（中文）
author: Tencent Cloud, Inc. # 作者的名字
org: Tencent Cloud, Inc. # 组织名称，可选
description:  This demo need to config COS trigger. When object has been upload to COS bucket, the SCF will download it to /tmp and compress it.  # 英文描述
description-i18n:
  zh-cn: 本示例配置了 COS 触发器，当有图片上传到对应的 COS bucket时，会触发云函数执行，并把文件下载到本地临时目录进行压缩处理，压缩后的图片会上传到另外一个bucket中 # 中文描述
type: template #类型：模版
keywords: tencent, scf, COS, Nodejs6.10, thumbnail # 关键字
repo: https://github.com/tencentyun/serverless-demo/tree/master/Nodejs6.10-Thumbnail # 源代码
readme: https://github.com/tencentyun/serverless-demo/tree/master/Nodejs6.10-Thumbnail/README.md # 详细的说明文件
license: MIT # 版权声明
src: # 描述项目中的哪些文件需要作为模板发布
  src: ./src # 指定具体的相对目录，此目录下的文件将作为模板发布
  exclude: #描述在指定的目录内哪些文件应该被排除
    # 通常你希望排除
    # 1. 包含secrets的文件
    # 2. .git git源代码管理的相关文件
    # 3. node_modules等第三方依赖文件