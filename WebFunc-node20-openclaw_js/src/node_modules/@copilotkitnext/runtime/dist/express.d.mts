import { Router } from 'express';
import { C as CopilotRuntime } from './runtime-BsuyYT2l.mjs';
import '@copilotkitnext/shared';
import '@ag-ui/client';
import 'rxjs';

interface CopilotExpressEndpointParams {
    runtime: CopilotRuntime;
    basePath: string;
}
declare function createCopilotEndpointExpress({ runtime, basePath }: CopilotExpressEndpointParams): Router;

interface CopilotSingleRouteExpressParams {
    runtime: CopilotRuntime;
    basePath: string;
}
declare function createCopilotEndpointSingleRouteExpress({ runtime, basePath, }: CopilotSingleRouteExpressParams): Router;

export { createCopilotEndpointExpress, createCopilotEndpointSingleRouteExpress };
