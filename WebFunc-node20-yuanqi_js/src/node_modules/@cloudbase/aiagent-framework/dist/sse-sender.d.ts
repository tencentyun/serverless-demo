import { sse } from '@cloudbase/functions-typings';
import { TcbContext } from './types';
/**
 * SSESender - 用于发送 Server-Sent Events
 */
export declare class SSESender<T> {
    readonly context: TcbContext;
    private _status;
    private _sse;
    get status(): "initial" | "started" | "ended";
    constructor(context: TcbContext);
    get sse(): sse.IServerSentEvent;
    send(data: sse.Event<T>): void;
    end(): void;
}
