"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.BotCore = void 0;
const tcb = require("@cloudbase/node-sdk");
const aitools_1 = require("./aitools");
const consts_1 = require("./consts");
const consts_2 = require("./consts");
const sse_sender_1 = require("./sse-sender");
const utils_1 = require("./utils");
/**
 * BotCore - 空白基类，实现了基本的初始化逻辑，配合 IBot 使用
 *
 * ```ts
 * import { BotCore, IBot } from '@cloudbase/aiagent-framework'
 * class Bot extends BotBase implements IBot {
 *   实现 IBot 定义的方法
 * }
 * ```
 */
class BotCore {
    /**
     * botTag - botTag
     */
    get botTag() {
        const url = this.context.httpContext.url;
        return (0, utils_1.parseBotTag)(url);
    }
    /**
     * botId - botId
     */
    get botId() {
        const url = this.context.httpContext.url;
        return (0, utils_1.parseBotId)(url);
    }
    get chatRecord() {
        return this.tcb.models[this.chatRecordDataModelKey];
    }
    // lazy，不需要 node sdk 的 Agent 本地开发时可以不用配 envId
    get tcb() {
        var _a, _b, _c, _d, _e, _f, _g;
        if (this._tcb)
            return this._tcb;
        const envId = (_a = this.context.extendedContext) === null || _a === void 0 ? void 0 : _a.envId;
        if (!envId)
            throw new Error('Invalid envId');
        const secretId = ((_c = (_b = this.context.extendedContext) === null || _b === void 0 ? void 0 : _b.tmpSecret) === null || _c === void 0 ? void 0 : _c.secretId) || process.env.TENCENTCLOUD_SECRETID; // 本地开发从环境变量读
        const secretKey = ((_e = (_d = this.context.extendedContext) === null || _d === void 0 ? void 0 : _d.tmpSecret) === null || _e === void 0 ? void 0 : _e.secretKey) || process.env.TENCENTCLOUD_SECRETKEY; // 本地开发从环境变量读
        const token = (_g = (_f = this.context.extendedContext) === null || _f === void 0 ? void 0 : _f.tmpSecret) === null || _g === void 0 ? void 0 : _g.token;
        this._tcb = tcb.init({
            env: envId,
            secretId,
            secretKey,
            sessionToken: token
        });
        return this._tcb;
    }
    /**
     * context - TCB 函数的上下文
     */
    constructor(context, option) {
        this.context = context;
        this._tcb = null;
        const { chatRecordDataModelKey = consts_1.CHAT_RECORD_DATA_MODEL } = option || {};
        this.chatRecordDataModelKey = chatRecordDataModelKey;
        this.sseSender = new sse_sender_1.SSESender(context);
        this.tools = new aitools_1.AITools(context);
    }
    /**
     * 创建一个用户聊天记录
     */
    createUserRecord({ record }) {
        var _a, _b;
        return Object.assign({
            role: 'user',
            record_id: (0, utils_1.genRecordId)(),
            sender: (_a = this.context.extendedContext) === null || _a === void 0 ? void 0 : _a.userId,
            type: consts_2.BOT_TYPE_TEXT,
            origin_msg: '{}',
            trigger_src: consts_2.TRIGGER_SRC_TCB,
            bot_id: this.botId,
            recommend_questions: [],
            conversation: (_b = this.context.extendedContext) === null || _b === void 0 ? void 0 : _b.userId,
            trace_id: this.context.ctxId,
            async_reply: '',
            image: ''
        }, record);
    }
    /**
     * 创建一个 Agent 聊天记录
     */
    createBotRecord({ record }) {
        var _a, _b;
        return Object.assign({
            role: 'assistant',
            sender: (_a = this.context.extendedContext) === null || _a === void 0 ? void 0 : _a.userId,
            type: consts_2.BOT_TYPE_TEXT,
            content: '',
            origin_msg: '{}',
            trigger_src: consts_2.TRIGGER_SRC_TCB,
            bot_id: this.botId,
            recommend_questions: [],
            conversation: (_b = this.context.extendedContext) === null || _b === void 0 ? void 0 : _b.userId,
            trace_id: this.context.ctxId,
            async_reply: '',
            image: '',
            need_async_reply: false
        }, record);
    }
    /**
     * 创建一对 用户 - Agent 的聊天记录，并存到数据模型中。
     * 返回值中提供更新 Agent 聊天记录的方法。
     */
    createRecordPair({ userContent }) {
        return __awaiter(this, void 0, void 0, function* () {
            const botRecordId = (0, utils_1.genRecordId)();
            const userRecord = this.createUserRecord({
                record: {
                    content: userContent,
                    reply: botRecordId
                }
            });
            const botRecord = this.createBotRecord({
                record: {
                    record_id: botRecordId
                }
            });
            yield this.chatRecord.create({ data: userRecord });
            yield this.chatRecord.create({ data: botRecord });
            return {
                userRecord,
                botRecord,
                updateBotRecord: (data) => __awaiter(this, void 0, void 0, function* () {
                    return this.chatRecord.update({
                        data,
                        filter: {
                            where: {
                                record_id: {
                                    $eq: botRecordId
                                }
                            }
                        }
                    });
                })
            };
        });
    }
    /**
     * `IBot#getChatRecords` 的默认实现。
     * 从数据模型中获取聊天记录。
     */
    getChatRecords({ pageNumber, pageSize, sort }) {
        var _a;
        return __awaiter(this, void 0, void 0, function* () {
            const res = yield this.chatRecord.list({
                filter: {
                    where: {
                        $and: [
                            {
                                conversation: {
                                    $eq: (_a = this.context.extendedContext) === null || _a === void 0 ? void 0 : _a.userId
                                }
                            },
                            {
                                bot_id: {
                                    $eq: this.botId
                                }
                            }
                        ]
                    }
                },
                getCount: true,
                select: {
                    $master: true
                },
                orderBy: [
                    {
                        createdAt: sort
                    }
                ],
                pageSize,
                pageNumber
            });
            const ret = {
                recordList: res.data.records.map(x => (Object.assign(Object.assign({}, x), { botId: x.bot_id, recordId: x.record_id, role: x.role, content: x.content, conversation: x.conversation, type: x.type, triggerSrc: x.trigger_src, replyTo: x.reply_to, reply: x.reply, image: x.image, createTime: x.createdAt }))),
                total: res.data.total
            };
            return ret;
        });
    }
    /**
     * 获取聊天记录，并加以整理，保证：
     * - 列表中，最旧的聊天记录在第一位，最新的在最后一位
     * - 聊天记录以用户的消息开头
     * - 用户的消息与 Agent 的消息交替出现
     * - 默认情况下保证以 Agent 的消息结尾
     */
    getHistoryMessages(option) {
        return __awaiter(this, void 0, void 0, function* () {
            const { size = 20, removeLastUser = true } = option || {};
            const { recordList } = yield this.getChatRecords({
                pageNumber: 1,
                pageSize: size,
                sort: 'desc'
            });
            // 保证最旧的聊天记录在第一位，最新的在最后一位
            recordList.reverse();
            const ret = recordList
                .filter(x => x.content)
                .reduce((acc, cur) => {
                const isUser = acc.length % 2 === 0;
                isUser &&
                    cur.role === 'user' &&
                    acc.push(cur);
                !isUser &&
                    cur.role === 'assistant' &&
                    acc.push(cur);
                return acc;
            }, []);
            if (removeLastUser && ret.length > 0 && ret[ret.length - 1].role === 'user') {
                ret.pop();
            }
            return ret;
        });
    }
}
exports.BotCore = BotCore;
