import type { NextApiRequest, NextApiResponse } from 'next';
import '../types';
export declare type LogoutApiArgs = {
    redirectUri: string;
    appDomain: string;
};
export declare function createLogoutApi({ appDomain, redirectUri }: LogoutApiArgs): (req: NextApiRequest, res: NextApiResponse) => void;
