"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getWxMediaContent = exports.sendWxClientMessage = exports.getTextToSpeech = exports.textToSpeech = exports.speechToText = exports.searchFile = exports.searchNetwork = exports.searchKnowledgeBase = exports.searchDB = void 0;
const node_util_1 = require("node:util");
const debug = (0, node_util_1.debuglog)('@cloudbase/aiagent-framework:tools');
function searchDB(baseURL, token, botId, msg, databaseModel) {
    var _a;
    return __awaiter(this, void 0, void 0, function* () {
        const url = `${baseURL}/v1/aibot/tool/chat-db`;
        let searchResult;
        const callToolsResult = yield callStreamTool(url, token, {
            botId,
            msg,
            databaseModel
        }, (result) => {
            searchResult = result === null || result === void 0 ? void 0 : result.searchResult; // 解析 JSON 数据
        });
        if ((callToolsResult === null || callToolsResult === void 0 ? void 0 : callToolsResult.code) && ((_a = callToolsResult === null || callToolsResult === void 0 ? void 0 : callToolsResult.code) === null || _a === void 0 ? void 0 : _a.length) !== 0) {
            return Object.assign(Object.assign({}, callToolsResult), { searchResult: searchResult });
        }
        return { searchResult: searchResult };
    });
}
exports.searchDB = searchDB;
function searchKnowledgeBase(baseURL, token, botId, msg, knowledgeBase) {
    var _a;
    return __awaiter(this, void 0, void 0, function* () {
        const url = `${baseURL}/v1/aibot/tool/chat-knowledge`;
        const documents = [];
        const knowledgeBaseFile = [];
        const knowledgeMeta = [];
        const callToolsResult = yield callStreamTool(url, token, {
            botId,
            msg,
            knowledgeBase
        }, (result) => {
            documents.push(...result.documents);
            knowledgeBaseFile.push(...result.knowledgeBase);
            knowledgeMeta.push(...result.knowledgeMeta);
        });
        if ((callToolsResult === null || callToolsResult === void 0 ? void 0 : callToolsResult.code) && ((_a = callToolsResult === null || callToolsResult === void 0 ? void 0 : callToolsResult.code) === null || _a === void 0 ? void 0 : _a.length) !== 0) {
            return Object.assign(Object.assign({}, callToolsResult), { documents, knowledgeBase: knowledgeBaseFile, knowledgeMeta });
        }
        return { documents, knowledgeBase: knowledgeBaseFile, knowledgeMeta };
    });
}
exports.searchKnowledgeBase = searchKnowledgeBase;
function searchNetwork(baseURL, token, botId, msg) {
    var _a;
    return __awaiter(this, void 0, void 0, function* () {
        const url = `${baseURL}/v1/aibot/tool/search-network`;
        let fullContent = '';
        let searchInfo;
        const callToolsResult = yield callStreamTool(url, token, {
            botId,
            msg
        }, (result) => {
            var _a;
            fullContent = fullContent + ((_a = result === null || result === void 0 ? void 0 : result.content) !== null && _a !== void 0 ? _a : '');
            searchInfo = result === null || result === void 0 ? void 0 : result.searchInfo;
        });
        if ((callToolsResult === null || callToolsResult === void 0 ? void 0 : callToolsResult.code) && ((_a = callToolsResult === null || callToolsResult === void 0 ? void 0 : callToolsResult.code) === null || _a === void 0 ? void 0 : _a.length) !== 0) {
            return Object.assign(Object.assign({}, callToolsResult), { content: fullContent, searchInfo });
        }
        return {
            content: fullContent,
            searchInfo
        };
    });
}
exports.searchNetwork = searchNetwork;
/**
 * callChatFile 识别图片内容，返回图片内容的文字描述
 */
function searchFile(baseURL, token, botId, msg, files) {
    var _a;
    return __awaiter(this, void 0, void 0, function* () {
        const url = `${baseURL}/v1/aibot/tool/chat-file`;
        let fullContent = '';
        const callToolsResult = yield callStreamTool(url, token, {
            botId,
            msg,
            fileList: files
        }, (result) => {
            var _a;
            fullContent = fullContent + ((_a = result === null || result === void 0 ? void 0 : result.content) !== null && _a !== void 0 ? _a : '');
        });
        if ((callToolsResult === null || callToolsResult === void 0 ? void 0 : callToolsResult.code) && ((_a = callToolsResult === null || callToolsResult === void 0 ? void 0 : callToolsResult.code) === null || _a === void 0 ? void 0 : _a.length) !== 0) {
            return Object.assign(Object.assign({}, callToolsResult), { content: fullContent });
        }
        return { content: fullContent };
    });
}
exports.searchFile = searchFile;
/**
 * callChatFile 识别图片内容，返回图片内容的文字描述
 */
function callStreamTool(url, token, body, onData) {
    return __awaiter(this, void 0, void 0, function* () {
        try {
            const resp = yield fetch(url, {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                    Accept: 'text/event-stream',
                    Authorization: `Bearer ${token}`
                },
                body: JSON.stringify(body)
            });
            if (!resp.ok) {
                throw new Error(`HTTP error! status: ${resp.status}, statusText: ${resp.statusText}`);
            }
            if (!resp.body) {
                throw new Error('Response body is null or undefined');
            }
            const contentType = resp.headers.get('content-type');
            // 判断 Content-Type 是否为 JSON, 只有报错的时候 才会返回JSON
            if (contentType && contentType.includes('application/json')) {
                const result = yield resp.json();
                return {
                    code: result.code,
                    message: result.message
                };
            }
            const reader = resp.body.getReader();
            const decoder = new TextDecoder('utf-8');
            let loopDone;
            let txt = '';
            do {
                const { done, value } = yield reader.read();
                loopDone = done;
                if (loopDone) {
                    break;
                }
                txt += decoder.decode(value, { stream: true });
                debug('callStreamTool txt:', txt);
                // 处理接收到的完整事件，SSE 事件以双换行分隔
                const events = txt.split('\n\n');
                for (let i = 0; i < events.length - 1; i++) {
                    const event = events[i].trim();
                    if (event === 'data: [DONE]' || event === 'data:[DONE]') {
                        continue;
                    }
                    if (event.startsWith('data:')) {
                        const data = event.substring(5).trim(); // 获取 data: 后面的数据
                        const result = safeJsonParse(data); // 解析 JSON 数据
                        // TODO: 考虑处理异常
                        onData(result);
                    }
                }
                txt = events[events.length - 1];
            } while (!loopDone);
        }
        catch (error) {
            throw new Error(`callStreamTool: ${error}`);
        }
        return {
            code: '',
            message: ''
        };
    });
}
function safeJsonParse(jsonString, defaultValue = null) {
    try {
        return JSON.parse(jsonString);
        /* eslint-disable-next-line */
    }
    catch (e) {
        return defaultValue;
    }
}
function callTool(url, method, token, body) {
    return __awaiter(this, void 0, void 0, function* () {
        try {
            const options = {
                method: method,
                headers: {
                    'Content-Type': 'application/json',
                    Accept: 'application/json',
                    Authorization: `Bearer ${token}`
                }
            };
            const noBodyMethods = ['GET', 'HEAD'];
            const upperMethod = method.toUpperCase();
            if (!noBodyMethods.includes(upperMethod)) {
                options.body = JSON.stringify(body);
            }
            const resp = yield fetch(url, options);
            if (!resp.ok) {
                throw new Error(`HTTP error! status: ${resp.status}, statusText: ${resp.statusText}`);
            }
            if (!resp.body) {
                throw new Error('Response body is null or undefined');
            }
            const result = yield resp.json();
            return result;
        }
        catch (error) {
            throw new Error(`callTool: ${error}`);
        }
    });
}
function speechToText(baseURL, token, botId, engSerViceType, voiceFormat, voiceUrl) {
    return __awaiter(this, void 0, void 0, function* () {
        const url = `${baseURL}/v1/aibot/tool/speech-to-text`;
        return yield callTool(url, 'POST', token, {
            botId,
            engSerViceType,
            voiceFormat,
            url: voiceUrl
        });
    });
}
exports.speechToText = speechToText;
function textToSpeech(baseURL, token, botId, text, voiceType) {
    return __awaiter(this, void 0, void 0, function* () {
        const url = `${baseURL}/v1/aibot/tool/text-to-speech`;
        return yield callTool(url, 'POST', token, {
            botId,
            text,
            voiceType
        });
    });
}
exports.textToSpeech = textToSpeech;
function getTextToSpeech(baseURL, token, botId, taskId) {
    return __awaiter(this, void 0, void 0, function* () {
        const url = `${baseURL}/v1/aibot/tool/text-to-speech?botId=${botId}&taskId=${taskId}`;
        return yield callTool(url, 'GET', token, {});
    });
}
exports.getTextToSpeech = getTextToSpeech;
function sendWxClientMessage(baseURL, token, botId, triggerSrc, wxClientMessage) {
    return __awaiter(this, void 0, void 0, function* () {
        const url = `${baseURL}/v1/aibot/tool/wx-client-message`;
        return yield callTool(url, 'POST', token, { botId, triggerSrc, wxClientMessage });
    });
}
exports.sendWxClientMessage = sendWxClientMessage;
function getWxMediaContent(baseURL, token, botId, triggerSrc, media) {
    return __awaiter(this, void 0, void 0, function* () {
        const url = `${baseURL}/v1/aibot/tool/wx-media-content?botId=${encodeURIComponent(botId)}&triggerSrc=${encodeURIComponent(triggerSrc)}&media=${encodeURIComponent(media)}`;
        return yield callTool(url, 'GET', token, {});
    });
}
exports.getWxMediaContent = getWxMediaContent;
