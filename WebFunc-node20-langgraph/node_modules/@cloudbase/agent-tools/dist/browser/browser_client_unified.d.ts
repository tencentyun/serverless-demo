import { Browser, BrowserContext } from 'playwright';
import { BrowserToolCall, BrowserToolResult } from './browser-tool-executor';
/**
 * Unified browser client configuration
 */
export interface BrowserClientConfig {
    mode: 'local' | 'sandbox';
    browser?: string;
    caps?: string[];
    allowedHosts?: string[];
    allowedOrigins?: string[];
    blockedOrigins?: string[];
    blockServiceWorkers?: boolean;
    timeout?: number;
    apiKey?: string;
    domain?: string;
    mcpServerPort?: number;
    sessionId?: string;
    maxInstances?: number;
    instanceTtl?: number;
    cleanupInterval?: number;
}
/**
 * MCP tool call interface (alias for BrowserToolCall)
 */
export interface MCPToolCall extends BrowserToolCall {
}
/**
 * MCP tool result interface (alias for BrowserToolResult)
 */
export interface MCPToolResult extends BrowserToolResult {
}
/**
 * Unified Browser Client Factory
 * Creates the appropriate client based on configuration
 */
export declare class UnifiedBrowserClient {
    private client;
    constructor(config: BrowserClientConfig);
    start(): Promise<void>;
    stop(): Promise<void>;
    callTool(toolCall: MCPToolCall): Promise<MCPToolResult>;
    listTools(): Promise<any[]>;
    isReady(): boolean;
    getVncUrl(): string | undefined;
    getCdpUrl(): string | undefined;
    getBrowser(): Browser | null;
    getContext(): BrowserContext | null;
    on(event: string, listener: (...args: any[]) => void): this;
    off(event: string, listener: (...args: any[]) => void): this;
}
//# sourceMappingURL=browser_client_unified.d.ts.map