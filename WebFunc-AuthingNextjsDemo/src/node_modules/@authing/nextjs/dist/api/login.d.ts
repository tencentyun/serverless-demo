import type { NextApiRequest, NextApiResponse } from 'next';
export declare type LoginApiArgs = {
    appDomain: string;
    clientId: string;
    redirectUri: string;
    scope: string;
};
export declare function createLoginApi({ appDomain, clientId, redirectUri, scope }: LoginApiArgs): (req: NextApiRequest, res: NextApiResponse) => void;
