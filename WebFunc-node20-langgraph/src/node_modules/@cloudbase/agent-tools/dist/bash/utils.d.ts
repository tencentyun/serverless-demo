import { BashExecutionContext } from "./bash-tool";
import { Sandbox } from "@e2b/code-interpreter";
/**
 * Create a local bash execution context
 */
export declare function createLocalBashContext(options?: {
    cwd?: string;
    env?: NodeJS.ProcessEnv;
    defaultTimeout?: number;
}): BashExecutionContext;
/**
 * Create a sandbox bash execution context
 */
export declare function createSandboxBashContext(options: {
    sandbox: Sandbox;
    cwd?: string;
    env?: Record<string, string>;
    defaultTimeout?: number;
}): BashExecutionContext;
/**
 * Validate command for security (basic checks)
 */
export declare function validateCommand(command: string): {
    isValid: boolean;
    reason?: string;
};
/**
 * Escape shell arguments
 */
export declare function escapeShellArg(arg: string): string;
/**
 * Build command with escaped arguments
 */
export declare function buildCommand(command: string, args?: string[]): string;
/**
 * Parse command output for common patterns
 */
export declare function parseCommandOutput(stdout: string, stderr: string): {
    lines: string[];
    errorLines: string[];
    isEmpty: boolean;
    hasErrors: boolean;
    exitedCleanly: boolean;
};
/**
 * Format execution time
 */
export declare function formatExecutionTime(ms: number): string;
/**
 * Common command builders
 */
export declare const CommandBuilders: {
    /**
     * List files with details
     */
    listFiles: (path?: string, options?: {
        all?: boolean;
        long?: boolean;
        human?: boolean;
    }) => string;
    /**
     * Find files by pattern
     */
    findFiles: (pattern: string, path?: string) => string;
    /**
     * Search in files
     */
    grep: (pattern: string, files?: string, options?: {
        recursive?: boolean;
        ignoreCase?: boolean;
        lineNumbers?: boolean;
    }) => string;
    /**
     * Check if file/directory exists
     */
    exists: (path: string) => string;
    /**
     * Get file info
     */
    stat: (path: string) => string;
    /**
     * Create directory
     */
    mkdir: (path: string, options?: {
        parents?: boolean;
    }) => string;
    /**
     * Copy files
     */
    copy: (source: string, destination: string, options?: {
        recursive?: boolean;
        preserve?: boolean;
    }) => string;
    /**
     * Move/rename files
     */
    move: (source: string, destination: string) => string;
    /**
     * Remove files/directories
     */
    remove: (path: string, options?: {
        recursive?: boolean;
        force?: boolean;
    }) => string;
};
//# sourceMappingURL=utils.d.ts.map