import "reflect-metadata";
import { LangChainAdapter } from "../langchain/langchain-adapter.mjs";

//#region src/service-adapters/bedrock/bedrock-adapter.d.ts
interface BedrockAdapterParams {
  /**
   * AWS Bedrock model ID to use.
   * @default "amazon.nova-lite-v1:0"
   */
  model?: string;
  /**
   * AWS region where Bedrock is available.
   * @default "us-east-1"
   */
  region?: string;
  /**
   * AWS credentials for Bedrock access.
   */
  credentials?: {
    accessKeyId?: string;
    secretAccessKey?: string;
  };
}
declare class BedrockAdapter extends LangChainAdapter {
  provider: string;
  model: string;
  constructor(options?: BedrockAdapterParams);
}
//#endregion
export { BedrockAdapter, BedrockAdapterParams };
//# sourceMappingURL=bedrock-adapter.d.mts.map