import { TcbContext } from '../types';
import * as tools from './tools';
export * from './tools';
export declare class AITools {
    private baseURL;
    private token;
    constructor(context: TcbContext);
    searchFile(botId: string, msg: string, files: string[]): Promise<tools.SearchFileResult>;
    searchDB(botId: string, msg: string, databaseModel: string[]): Promise<tools.SearchDBResult>;
    searchKnowledgeBase(botId: string, msg: string, knowledgeBase: string[]): Promise<tools.SearchKnowledgeResult>;
    searchNetwork(botId: string, msg: string): Promise<tools.SearchNetworkResult>;
    speechToText(botId: string, engSerViceType: string, voiceFormat: string, voiceUrl: string): Promise<tools.SpeechToTextResult>;
    textToSpeech(botId: string, text: string, voiceType: number): Promise<tools.TextToSpeechResult>;
    getTextToSpeech(botId: string, taskId: string): Promise<tools.GetTextToSpeechResult>;
    getWxMediaContent(botId: string, triggerSrc: string, media: string): Promise<tools.GetWxMediaContentResult>;
    sendWxClientMessage(botId: string, triggerSrc: string, wxClientMessage: tools.WxClientMessageDto): Promise<tools.toolsCommonResp>;
}
