component: scf
name: ap-guangzhou_default_COSSQLServerBackup
inputs:
  src:
    src: ./src
  handler: index.main_handler
  memorySize: 1024
  name: COSSQLServerBackup
  region: ap-guangzhou
  runtime: Nodejs12.16
  timeout: 900
  environment:
    variables:
      instanceList: instanceList
      targetBucket: targetBucket
      targetRegion: targetRegion
      targetPrefix: targetPrefix