"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.transformKeysToCamelCase = exports.genRandomStr = exports.genRecordId = exports.parseQuery = exports.parseQueryFromCtx = exports.parseEnvFromHeader = exports.parseWxEnvParam = exports.parseApiName = exports.parseBotTag = exports.parseConversationId = exports.parseBotId = void 0;
const crypto = require("crypto");
const BOT_REG = /\/v1\/aibot\/bots\/([^/]+)(\/)*([^\/]+)*(\/)*([^\/]+)*/;
const SPECIAL_KEY = new Map();
// 添加键值对
SPECIAL_KEY.set('msgid', 'msgId');
SPECIAL_KEY.set('openKfid', 'openKfId');
SPECIAL_KEY.set('externalUserid', 'externalUserId');
SPECIAL_KEY.set('msgtype', 'msgType');
function parseBotId(url) {
    return parseUrl(url).botId;
}
exports.parseBotId = parseBotId;
function parseConversationId(url) {
    const { pathname } = new URL(url);
    // /v1/aibot/bots/:botId/conversation/:conversationId
    const parts = pathname.split('/');
    if (!parts || parts.length < 7) {
        return '';
    }
    return parts[6];
}
exports.parseConversationId = parseConversationId;
function parseBotTag(url) {
    const botId = parseBotId(url);
    const botTag = botId.split('-')[2]; // expect botId to be `ibot-xxx-${botTag}`
    return botTag;
}
exports.parseBotTag = parseBotTag;
function parseApiName(url) {
    try {
        return parseUrl(url).apiName;
    }
    catch (e) {
        return null;
    }
}
exports.parseApiName = parseApiName;
function parseUrl(url) {
    const { pathname } = new URL(url);
    const regResult = pathname.match(BOT_REG);
    if (!regResult) {
        throw new Error('Invalid pathname');
    }
    const [_, botId, _p, apiName] = regResult;
    return {
        botId,
        apiName: apiName || ''
    };
}
function parseWxEnvParam(ctx) {
    const envMap = parseEnvFromHeader(ctx, 'x-scf-environments');
    return {
        triggerSrc: envMap['WX_APPTYPE'],
        wxVerify: envMap['WX_QUALIFICATION_VERIFY'] === '1' ? true : false
    };
}
exports.parseWxEnvParam = parseWxEnvParam;
function parseEnvFromHeader(ctx, key) {
    var _a;
    const headers = (_a = ctx.httpContext) === null || _a === void 0 ? void 0 : _a.headers;
    if (!headers) {
        throw new Error('Invalid headers');
    }
    const value = headers[key];
    const envMap = value.split(',').reduce((env, pair) => {
        const [key, value] = pair.split('=');
        env[key.trim()] = value.trim();
        return env;
    }, {});
    return envMap;
}
exports.parseEnvFromHeader = parseEnvFromHeader;
function parseQueryFromCtx(ctx) {
    var _a;
    const url = (_a = ctx.httpContext) === null || _a === void 0 ? void 0 : _a.url;
    if (!url) {
        throw new Error('Invalid url: ' + url);
    }
    return parseQuery(url);
}
exports.parseQueryFromCtx = parseQueryFromCtx;
function parseQuery(url) {
    const { search } = new URL(url);
    const query = new URLSearchParams(search);
    const ret = {};
    for (const [key, value] of query.entries()) {
        ret[key] = value;
    }
    return ret;
}
exports.parseQuery = parseQuery;
function genRecordId() {
    return 'record-' + genRandomStr(8);
}
exports.genRecordId = genRecordId;
/**
 * 生成随机字符串
 */
function genRandomStr(length) {
    // 生成随机的字节数组
    const randomBytes = crypto.randomBytes(Math.ceil(length / 2));
    // 将字节数组转换为十六进制字符串
    const hexString = randomBytes.toString('hex').slice(0, length);
    return hexString;
}
exports.genRandomStr = genRandomStr;
/**
 * 将字符串转换为大驼峰格式
 */
function toPascalCase(str) {
    // 处理开头是下划线的情况
    if (str.startsWith('_')) {
        return '_' + toPascalCase(str.slice(1));
    }
    // 处理普通情况：转换所有分隔符后的字母为大写
    return str.replace(/(^\w|[-_]\w)/g, match => {
        return match.replace(/[-_]/, '').toUpperCase();
    });
}
function toCamelCase(str) {
    // 处理开头是下划线的情况
    if (str.startsWith('_')) {
        return '_' + toCamelCase(str.slice(1));
    }
    // 处理普通情况：先转换为PascalCase，再将首字母小写
    const pascal = toPascalCase(str);
    return pascal.charAt(0).toLowerCase() + pascal.slice(1);
}
/**
 * 递归转换对象的所有key为小驼峰格式
 */
function transformKeysToCamelCase(obj) {
    if (Array.isArray(obj)) {
        return obj.map(item => transformKeysToCamelCase(item));
    }
    if (obj !== null && typeof obj === 'object') {
        const newObj = {};
        Object.keys(obj).forEach(key => {
            let newKey = toCamelCase(key);
            // 企业微信的接口不符合命名规范，手动处理一下
            if (SPECIAL_KEY.has(newKey)) {
                newKey = SPECIAL_KEY.get(newKey);
            }
            newObj[newKey] = transformKeysToCamelCase(obj[key]);
        });
        return newObj;
    }
    return obj;
}
exports.transformKeysToCamelCase = transformKeysToCamelCase;
